# Deployment Configuration Tasks

- [x] Configure Frontend
  - [x] Update `App.js` to use `REACT_APP_API_URL` environment variable
  - [x] Create `netlify.toml` in `frontend/` directory for Netlify configuration
- [x] Configure Backend
  - [x] Update `app.py` to use dynamic `request.host_url` for downloads
- [x] Local Verification
  - [x] Test frontend and backend locally to ensure they still communicate properly
- [x] Documentation & Hand-off
  - [x] Provide step-by-step instructions for deploying to Netlify and a suitable backend hosting platform (e.g. Render)
